package unex.cume.mdai.SendaLite.config;

// Este archivo se deja vacío/intencionalmente inactivo para no registrar un bean
// passwordEncoder que colisione con el definido en SecurityConfig.
// Si necesitas volver a activar el bean, añade @Configuration y el método @Bean otra vez.

public final class PasswordEncoderConfig {
    private PasswordEncoderConfig() { /* helper class - no beans */ }
}
